version: 2
#documento para rastrear as fontes de dados
sources:
  - name: stripe
    database: raw
    schema: stripe
    tables:
      - name: payments
        description:  Raw payment data from Stripe